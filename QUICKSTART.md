# 🚀 快速开始指南

欢迎开始开发**驾考刷刷**项目！

---

## ✅ 项目初始化已完成

以下工作已经完成：

### 1. 项目结构 ✅
- 根目录结构
- 后端项目（Golang）
- 前端项目（Flutter）
- 文档目录

### 2. 后端框架 ✅
- Gin 框架配置
- 路由和中间件
- 服务层框架
- 数据模型
- 题库元数据

### 3. 前端框架 ✅
- Flutter 项目结构
- 依赖配置（pubspec.yaml）
- 主题系统（浅色/深色）
- 应用配置
- 常量定义

### 4. 文档 ✅
- 产品需求文档（PRD.md）
- 数据结构文档（data-schema.md）
- API 文档（API.md）
- 任务清单（TASKS.md）
- 项目结构（PROJECT_STRUCTURE.md）

---

## 📂 项目位置

```bash
/Users/gavin/driving-quiz-app/
```

---

## 🎯 下一步：立即可以开始的任务

### 优先级 1：准备数据（无依赖）

#### Task A: 创建 Demo 题库 
**目标**: 创建 50 题的示例题库

**文件位置**:
- `backend/data/banks/demo_bank.json`
- `mobile/assets/data/demo_bank.json`

**任务内容**:
- 单选题 20 题
- 多选题 15 题
- 判断题 15 题

**参考**: `docs/data-schema.md`

**Agent 提示词**:
```
请帮我创建 Demo 题库数据文件。

文件路径: backend/data/banks/demo_bank.json
要求:
- 总共50题
- 单选题20题（type: "single"）
- 多选题15题（type: "multiple"）
- 判断题15题（type: "judge"）
- 题目内容为驾考相关
- 每题包含：题目、选项、答案、解析

数据格式参考 docs/data-schema.md
```

---

#### Task B: 创建科目一题库样例
**目标**: 创建至少 100 题的科目一题库样例

**文件位置**: `backend/data/banks/cn_subject1_v1.json`

**Agent 提示词**:
```
请帮我创建科目一题库数据文件（100题样例）。

文件路径: backend/data/banks/cn_subject1_v1.json
题库ID: cn_subject1_v1
题库名称: 中国驾照科目一
题目数量: 100题（可以更多）

数据格式参考 docs/data-schema.md
```

---

### 优先级 2：后端 API 开发（需要数据准备完成）

#### Task C: 实现题库列表 API
**依赖**: Task A, B 完成

**Agent 提示词**:
```
请帮我实现后端题库列表 API。

需要修改的文件:
- backend/main.go (取消注释路由)
- backend/api/handler.go (完善 GetBankList 函数)
- backend/service/bank_service.go (确保服务正常)

功能要求:
1. GET /api/banks 返回所有题库列表
2. 从 data/banks.json 读取元数据
3. 为每个题库生成 download_url
4. 错误处理

测试:
curl http://localhost:8080/api/banks

参考: docs/API.md
```

---

#### Task D: 实现题库下载 API
**依赖**: Task A, B, C 完成

**Agent 提示词**:
```
请帮我实现题库下载 API。

需要修改的文件:
- backend/main.go (取消注释路由)
- backend/api/handler.go (完善 DownloadBank 函数)

功能要求:
1. GET /api/banks/:id/download 返回题库JSON文件
2. 设置正确的响应头（Content-Disposition）
3. 处理题库不存在的情况（404）

测试:
curl http://localhost:8080/api/banks/demo_bank/download -o demo.json

参考: docs/API.md
```

---

### 优先级 3：前端数据层（可与后端并行）

#### Task E: 配置 Drift 数据库
**Agent 提示词**:
```
请帮我配置 Flutter 的 Drift 数据库。

需要创建的文件:
1. mobile/lib/core/database/database.dart
   - 定义 AppDatabase 类
   - 配置数据库连接
   
2. mobile/lib/core/database/tables/ 下创建5个表定义:
   - question_banks.dart
   - answer_records.dart
   - bank_progress.dart
   - wrong_questions.dart
   - favorites.dart

表结构参考: docs/data-schema.md

完成后运行:
cd mobile
flutter pub run build_runner build --delete-conflicting-outputs
```

---

#### Task F: 实现网络请求层
**Agent 提示词**:
```
请帮我实现 Flutter 的网络请求层。

需要创建的文件:
1. mobile/lib/core/network/api_client.dart
   - 封装 Dio
   - 配置超时、拦截器
   - 提供 getBankList() 和 downloadBank() 方法

2. mobile/lib/core/network/api_endpoints.dart
   - 定义所有 API 端点常量

配置参考: mobile/lib/config/app_config.dart
API 参考: docs/API.md
```

---

## 🔧 开发环境检查

### 后端开发环境

```bash
# 检查 Golang 版本
go version
# 需要: go1.21 或更高

# 进入后端目录
cd backend

# 下载依赖
go mod download

# 运行服务
go run main.go

# 测试
curl http://localhost:8080/api/health
```

### 前端开发环境

```bash
# 检查 Flutter 版本
flutter --version
# 需要: Flutter 3.19+ / Dart 3.3+

# 进入前端目录
cd mobile

# 获取依赖
flutter pub get

# 检查设备
flutter devices

# 运行（需要 Flutter SDK 和设备/模拟器）
# flutter run
```

---

## 📝 使用 Agent 开发的建议

### 1. 单任务原则
每次给 Agent 一个明确的任务，完成后再进行下一个。

### 2. 提供上下文
在提示词中包含：
- 文件路径
- 功能要求
- 参考文档
- 测试方法

### 3. 任务优先级
建议按照上面的优先级顺序进行：
1. 先准备数据（Task A, B）
2. 再开发后端 API（Task C, D）
3. 最后开发前端（Task E, F 可并行）

### 4. 验证和测试
每完成一个任务，立即测试验证。

---

## 📊 任务追踪

可以在 `docs/TASKS.md` 中查看和更新任务进度。

每完成一个任务，将状态从 `[ ]` 改为 `[x]`。

---

## 🆘 遇到问题？

### 文档参考
- **PRD**: `docs/PRD.md` - 完整的产品需求
- **数据结构**: `docs/data-schema.md` - 数据格式定义
- **API 文档**: `docs/API.md` - API 接口说明
- **项目结构**: `PROJECT_STRUCTURE.md` - 完整目录说明

### 代码示例
- **后端**: `backend/` 目录下的 README 和代码
- **前端**: `mobile/` 目录下的 README 和代码

---

## 🎉 开始开发

推荐从 **Task A（创建 Demo 题库）** 开始！

```bash
# 进入项目目录
cd /Users/gavin/driving-quiz-app

# 查看项目结构
ls -la

# 开始第一个任务
# 创建 backend/data/banks/demo_bank.json
```

祝开发顺利！🚀
